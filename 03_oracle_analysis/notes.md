# 03 Oracle Analysis

## Process

- Cell 1: filterwarnings
- Cell 3: trade_counts; oracle_counts
- Cell 4: plt.savefig(output_path, dpi
- Cell 7: slot_to_datetime; pool_trade_stats; pool_oracle_stats; oracle_slot_patterns; pool_analysis
- Cell 10: events_per_sec; trade_counts; oracle_counts; burst_trades.to_csv(burst_trades_output, index; burst_oracles.to_csv(burst_oracles_output, index
- Cell 11: plt.savefig(output_path, dpi
- Cell 12: burst_patterns.to_csv(pattern_output, index
- Cell 13: plt.savefig(cluster_viz_path, dpi

## Results

### Generated Files

- No output files found. Run the notebook to generate outputs.

